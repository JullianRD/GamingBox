-- Relation Merise : users (0,N) (un utilisateur peut ou non créer une à plusieurs reviews) --- (1,1) items (une review appartient à un utilisateur)
-- ============================================================================

-- Forcer l'encodage client
SET CLIENT_ENCODING TO 'UTF8';